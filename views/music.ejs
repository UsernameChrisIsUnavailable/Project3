<%- include partials/header.ejs %>
<%- include partials/top_nav.ejs %>
<!--Main content-->
<main>
            <div class="jumbotron">
            <h1 class="display-4"><p>Welcome to Chris' <%= title %> page!</p></h1>
            
            <a class="btn btn-primary btn-lg" href="#" role="button">Learn more</a>
            </div>


<div class ="container">
    <div class="row">
        <div class="col-md-offset-3 col-md-6">
            <h1><%= title %></h1>
            <br>
    <table class="table table-bordered table-striped table-hover">
    <tr>
        <th class="text-center">Song</th>  
        <th class="text-center">Artist</th>   
        <th class="text-center">Year</th>   
        <th class="text-center">Genre</th>   
        <th class="text-center">Rating</th>   
    </tr>

    <!--repeats the template-->
    <% for(let count=0; count<musiclist.length; count++) { %>
        <tr>
            <td class="text-center"><%= musiclist[count].Song %></td>
            <td class="text-center"><%= musiclist[count].Artist %></td>
            <td class="text-center"><%= musiclist[count].Year %></td>
            <td class="text-center"><%= musiclist[count].Genre %></td>
            <td class="text-center"><%= musiclist[count].Rating %></td>
        </tr>
    <%} %>
 </table>
</div>

</main>

<%- include partials/bot_nav.ejs %>
  
    <!--these are the script files-->
    <script src = "scripts/app.js"></script>
    <script src = "/bootstrap/dist/js/bootstrap.min.js"></script>
    <script src = "/jquery/dist/jquery.min.js"></script>
</body>
</html>
